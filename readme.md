# tuie-demo

Interactive showcase of the [tuie](https://crates.io/crates/tuie) widget toolkit.

Run as a terminal app:

```
cargo run -p tuie-demo
```

Run as a native GUI:

```
cargo run -p tuie-demo --features gui
```
